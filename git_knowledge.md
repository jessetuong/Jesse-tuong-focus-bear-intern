## Have I used Git before?
Yes — I use Git regularly via the integrated terminal in VS Code, running commands like `git add`, `git commit`, and `git push` directly rather than using a GUI client.

## Which Git client did I choose?
I don't use a dedicated GUI client day-to-day — I run Git commands through VS Code's built-in terminal. I did try GitHub Desktop for the merge conflict exercise, since resolving conflicts is easier with a visual diff view.

## Most interesting thing I learned
[Fill in — e.g., something about branching, merge conflicts, or a command you hadn't used before]